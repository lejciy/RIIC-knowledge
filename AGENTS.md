# AGENTS.md — Agent 接入入口

> 本文件面向以任意 agent 工具使用本仓库的读者，说明知识语料范围与检索路径。仓库总览与数据源见根目录 `README.md`，本文件只做检索指引。

## 这个仓库是什么

明日方舟基建 RAG 知识库。知识正文是 `docs/` 下的 7 个内容模块（110 篇），配套 `skill/` 场景问答手册与 `agent/SYSTEM_PROMPT.md` 行为总则。`skill/`、`index/`、`agent/` 属行为与检索支撑层，不是知识语料。

## 建议读取顺序

1. 共享知识总则：`agent/KNOWLEDGE_RULES.md`；独立入口通过 `agent/load-policy.mjs` 展开 `agent/SYSTEM_PROMPT.md`，网站读取同一总则并自行维护工具编排。
2. 检索前置：`index/消歧字典.json` —— 将玩家俗称归一为库内规范名；流程见 `skill/skill-1-检索路由与歧义消解.md`，该 skill 对所有检索类问题必走。
3. 模块路由：`index/导诊表.md` —— 按问题类型定位模块与入口文件。
4. 小节定位：`index/标题清单.md` —— 文件与小节级索引；干员定位走 `docs/2-干员技能/名册.md`。
5. 场景手册：`skill/manifest.json` 白名单按需加载 skill-1/3/4/6/7/9；2/5/8 保留草稿，不作为已启用计算能力。
6. 阅读 `docs/` 正文作答，答案附友好正文标题及必要小节，不显示内部路径。

## 消歧规则

- 俗称未归一前，不得按字面命中直接下结论；检索命中空结果时先换规范名重试，仍空才判定「库内没有」。
- 字典返回多候选时：设施可区分的按上下文消解并在回答中说明；设施重叠或无法判断的，向用户列出候选并询问。
- 子串同名对（如「临光」与「耀骑士临光」）按字典内 `resolution` 字段处理，不得静默二选一。
- 带空格或间隔号的输入先用 `normalized_keys` 归一（如「裁缝 β」→ 裁缝·β）。

## 数值边界

- 只引用 `docs/` 已载明的数值；跨资源换算仅沿 `docs/1-基础设定/资源体系/产出常数表.md` 注册的关系执行，并注明所用关系。
- 产出与回本仅引用已有正文结论；单步注册换算之外的核算交给宿主实际支持的计算工具。skill-2/5/8 仍为未启用草稿。
- 独立入口无计算工具时，完整排班可转介 riic.autos；网站入口直接调用已有工具并遵守其确认流程。

## 未查到的表述

保留可靠结果直接回答；成功检索无特殊释义时正常按技能原文回答，不报告检索覆盖。确实影响结论的缺口只说明具体不确定事项。工具故障不得伪称没有规则。

## 语料边界

- 入语料：`docs/**/*.md`。各模块 `README.md` 为 `type: index` 导航页，只用于路由，不承载知识正文。
- 不入语料：`skill/`、`index/`、`agent/`、`templates/`、`参考样例/`、`tests/` 与 `scripts/`，属行为、检索、写作与测试支撑层，作流程依据而非事实来源。
- `docs/2-干员技能/` 根目录快照为外部数据派生的只读产物，请勿手改；`释义/` 为人工维护区。

## 派生物声明

`index/消歧字典.json` 与 `index/标题清单.md` 由 `scripts/build-agent-index.mjs` 从正文生成，重新生成命令为 `node scripts/build-agent-index.mjs`，不手工编辑；与 `docs/` 正文冲突时以正文为准。
